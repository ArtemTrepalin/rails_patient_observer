= simple_form_for @contact, url: admin_contacts_path do |f|
  = f.input :name
  = f.input :phone_number
  = f.association :patient
  = f.button :submit, class: 'btn btn-info'
= link_to t('.back'), admin_contacts_path, class: 'btn btn-info'
