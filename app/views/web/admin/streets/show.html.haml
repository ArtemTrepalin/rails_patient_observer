%table.table
  %tr.table-info
    %th= Street.human_attribute_name(:name)
  %tbody
    %tr
      %td=@street.name
= link_to t('.back'), admin_streets_path, :class => "btn btn-info"
= link_to t('.delete'), admin_street_path(@street.id), method: :delete, :class => "btn btn-info"
= link_to t('.edit'), edit_admin_street_path(@street.id), :class => "btn btn-info"

