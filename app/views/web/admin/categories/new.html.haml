= simple_form_for @category, url: admin_categories_path do |f|
  = f.input :name
  = f.input :image_link
  = f.button :submit, class: 'btn btn-outline-secondary'
= link_to t('.back'), admin_categories_path, class: 'btn btn-outline-secondary'
