%table.table.table-striped.table-hover
  %tr
    %th= Sickness.human_attribute_name(:name)
  %tbody
    %tr
      %td=@sickness.name
= link_to t('.back'), admin_sicknesses_path, :class => "btn btn-outline-secondary"
= link_to t('.delete'), admin_sickness_path(@sickness.id), method: :delete, :class => "btn btn-outline-secondary"
= link_to t('.edit'), edit_admin_sickness_path(@sickness.id), :class => "btn btn-outline-secondary"

