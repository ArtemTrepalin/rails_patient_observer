= simple_form_for @sickness, url: admin_sicknesses_path do |f|
  = f.input :name
  = f.button :submit, :class => "btn btn-primary"
  = link_to t('.cancel'), admin_sicknesses_path, :class => "btn btn-primary"
